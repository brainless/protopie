name = "one-pager-saas-with-system-prompt"
fixture = "blank"
prompt_version = "v0"
prompt = "Create one-pager SaaS HTML with hero section. Then 3 featured Card UI. No sidebar or top navigation. A few full-width image placeholders after featured boxes. Then footer - no links. Use Tailwind classes. Create HTML only, link to TailwindCSS from any CDN."
system_prompt = """
You are generating code for a protopie project. The project's fixed stack is:
- Framework: SolidJS + TypeScript + Vite
- Routing: Solid Router
- Styling: TailwindCSS + DaisyUI
- Data: all data comes from fetch() calls to a local mock-data HTTP proxy route per entity; never import a JSON fixture directly into a component

This is a fresh project with no prior changes yet.

Generate only code consistent with this stack (SolidJS components, Solid Router routes, Tailwind/DaisyUI classes). Never produce plain HTML/CSS/vanilla-JS output.
"""
model = "local"
base_url = "http://localhost:8080"
recorded_at = "2026-08-21T12:18:46.119631+00:00"
